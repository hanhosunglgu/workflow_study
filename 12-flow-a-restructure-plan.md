# 플로우 A 워크플로우 재구성 계획서 — 컨텍스트 예산 문제의 근본 해결

**작성일**: 2026-08-20
**대상**: ixi-enterprise 캔버스의 IVMS 연동 플로우 A
**목적**: `context limit exceeded` 4회 연속 실패의 원인을 재규정하고, 워크플로우를 더 작은 단위로 분해하는 재구성안을 확정한다
**전제 조건**: 토큰 예산은 늘릴 수 없다. 현재 구조 안에서 분해로 해결해야 한다
**관련 문서**: `11-flow-a-node-detail-config.md`(노드 상세), `04-ixi-enterprise-node-catalog.md`(노드 카탈로그), `07-ixi-enterprise-requirements-spec.md`(플랫폼 요구사항), `ixi-enterprise/docs/ivms-flow-a-build-lessons.md`(구축 실전 기록), `ixi-enterprise/stage2_2-3-test-guide.md`(실패 이력 원본)

---

## 0. 요약

**Agent를 더 쪼개는 것은 답이 아니다.** 정적 데이터를 런타임에서 제거하고, "Agent 하나당 Tool 호출 1회"를 지키는 작은 워크플로우들로 재구성하는 것이 답이다.

> 🔴 **2026-08-20 실측**: 4-1절 측정 결과, 서버 측 필터(`filter.xorStr`)는 **동작하지 않는다**(MD5 대조로 확정). ~~대상 조직 전량 조회는 예산의 6.3배를 요구한다~~ → **4-2절에서 정정됨: 그 6.3배의 98.8%는 조직 미기입 garbage였다.**
>
> 🔵 **제약 확정 (2026-08-21)**: 해결은 **ixi-enterprise 캔버스 컴포넌트만으로** 이루어져야 한다. 외부 배치 서버·집계 API·MCP 서버 신설은 **범위 밖**이다. 따라서 남은 레버는 **처리 범위 자체를 줄이는 것**(유효성이 확인된 `mgmtOrgId` 축)뿐이다.
>
> 🔵 **4-2절 재측정으로 결론 반전 (2026-08-21)**: 4-1절의 "9,794건"은 실자산이 아니었다. **98.8%가 조직 ID 미기입 개발기 자산**이며, 실제 조직 자산은 **약 112건(예산의 12.8%)**이다. 조직 단위로 자르면 **계층 1이 1회 실행으로 완주**한다. 단 개발기에서는 garbage를 걸러낼 수단이 없어 실행 검증이 불가하고, **운영기 실규모 측정이 선결 과제**다(4-2.6절).

| 항목 | 내용 |
|---|---|
| 실패 원인 재규정 | 데이터 양이 아니라 **한 Agent 안의 Tool 호출 횟수** |
| 기각된 방향 | 예산 증액(9.45배 증가했으나 실패), stage 축 Agent 분할(4회 실패), **서버 측 필터(실측 무효)**, **API Request 일반 모드(캔버스에 부재)** |
| 채택 방향 | 정적 데이터 런타임 제거 + **조직 단위 범위 축소** + Agent당 호출 1회 |
| 실측 규모 | 조직 실자산 **약 112건**(1페이지). ~~9,794건~~은 98.8%가 조직 미기입 garbage (4-2절) |
| 실측 비용 | 1페이지 = **121,085 tok = 예산의 13%**. 조직 1개 = **12.8%** |
| 캔버스 상한 | 누적 없이 **7회 호출**, 규칙 ① 적용 시 **3회** |
| 운영 전제 | 캔버스 컴포넌트 한정. `mgmtOrgId` 조직 단위 조회 — **필수** |
| 🔴 선결 과제 | **운영기 실규모 측정** — 개발기는 garbage 때문에 실행 검증 불가 (4-2.6절) |

---

## 1. 진단 정정 — 문제는 "데이터가 많아서"가 아니다

### 1.1 예산은 이미 늘어났고, 그래도 실패했다

문서 다수가 예산을 `100,000`으로 서술하지만 이는 구 모델 기준이다. `ixi-enterprise/stage2_2-3-test-guide.md` 1.2-2절 기준 **현재 예산은 `945,000`**(gpt-5.5 전환)이다.

| 시점 | 추정 토큰 | 예산 | 모델 |
|---|---|---|---|
| 과거 1차 | 120,032 | 100,000 | 구 모델 |
| 과거 2차 | 109,508 | 100,000 | 구 모델 |
| 3차 | **1,324,532** | **945,000** | gpt-5.5 |

예산이 **9.45배 증가**했으나 사용량이 **11배**로 더 크게 늘어 여전히 초과했다.

> 🔴 **결론: 예산 증액 방향은 이미 실증적으로 시도됐고 실패했다.** 토큰을 늘릴 수 없다는 제약과 무관하게, 늘려도 해결되지 않는 문제다.

### 1.2 4회 실패의 지점 이동

| 실패 | 지점 | 추정 토큰 | 당시 대응 |
|---|---|---|---|
| 1차 | #1B (`mngtListDetail`) | 120,032 | Tool 5개 단일 Agent → Agent 3분할 |
| 2차 | #1B | 109,508 | `pageSize` 10→200, 반복상한 3회 |
| 3차 | #1B-2 (담당자별 재조회) | 1,324,532 | 담당자 1명/1페이지/30건으로 축소 |
| 4차 | **#1A (`orgList`)** | 1,017,179 | #1A 하드코딩(Tool 3개 제거) |

### 1.3 4차 실패가 결정적이다

4차에서 터진 Agent #1A는 **`orgList` GET 6회짜리이며, 자산 데이터를 한 건도 다루지 않는 노드**다. 그런데도 1,017,179 토큰을 소진했고 **#1B-2는 시작조차 하지 못했다.**

로그의 Thinking 내용에 누적 호출 수 증가가 그대로 드러난다.

```
"admin API 3건으로 조직ID 흐름을 좁혔고, 이제 템플릿·자산분류를 잇습니다"
"admin API 5건 호출로 조직ID 확인 후 템플릿·자산분류 조회를 이어갑니다"
"org 조회 API 6건 응답을 엮어 Enterprise SW프로덕트개발팀 조직ID를 확정 중입니다"
```

**자산 데이터가 전혀 없는 노드가 예산을 소진했다는 것은, 병목이 데이터 양이 아니라 호출 횟수라는 뜻이다.**

원본 기록의 교훈도 같은 방향이다.

> "축소 대상을 뒷단에서만 찾았으나 실제 병목은 **이미 검증을 마쳐 손대지 않던 앞단**에 있었다. 파이프라인이 길어지면 앞단의 비용이 뒤로 전파된다." — `stage2_2-3-test-guide.md` 1.2-4절

---

## 2. 토큰 회계 규칙 3개

재구성안의 타당성은 이 세 가지 전제 위에서 판단한다.

### 규칙 ① Agent 루프는 매 턴마다 이전 툴 응답 전체를 재전송한다 → 비용이 호출 횟수의 제곱에 가깝게 증가

Agent가 Tool을 N회 호출하면 응답 원문이 누적 재전송되어 `1+2+...+N = N(N+1)/2` 배로 청구된다.

| 호출 횟수 | 누적 배수 |
|---|---|
| 2회 | 3배 |
| 4회 | 10배 |
| 6회 | **21배** |
| 10회 | 55배 |

#1A가 작은 GET 6회로 1M 토큰을 태운 것이 이 메커니즘으로 설명된다.

> ⚠️ **이 규칙은 추론이며 미검증이다.** 플랫폼 추정기가 정확히 무엇을 합산하는지(해당 Agent만인지, 플로우 전체 프롬프트 총량인지)는 문서에 기록이 없고, REQ-009/010(Execution Log·Input/Output 패널) 부재로 확인 수단도 없다. `orgList` 응답 크기만으로는 1,017,179라는 수치가 완전히 설명되지 않는다. → **4절 교정 측정으로 먼저 검증할 것.**

### 규칙 ② Agent의 "요약해서 넘겨라" 지시는 절감 효과가 없다

요약은 **원문이 이미 컨텍스트에 들어온 뒤** 일어난다. #1B의 경량 스캔이 담당자 ID만 남기도록 지시해도, 버려지는 자산 상세는 이미 토큰을 전부 소비한 상태다.

절감되는 것은 *다음* Agent로 넘어가는 비용뿐이고, 터지는 지점은 그 Agent 자신이다. 2·3차 실패가 이를 실증한다.

### 규칙 ③ 캔버스에는 projection 지점이 없다

응답 원문을 줄이려면 두 가지 중 하나가 필요하다.

| 수단 | 가용성 |
|---|---|
| 서버가 필드를 골라주기(projection) | ❌ IVMS API에 필드 선택 파라미터 없음 |
| 중간 노드에서 잘라내기 | ❌ Function/Code/Loop/Merge 노드 부재(`04-ixi-enterprise-node-catalog.md`) |

**따라서 응답 원문이 Agent 컨텍스트에 통째로 들어오는 것을 막을 수단이 현재 구조에 존재하지 않는다.**

### 세 규칙의 결론

해법은 반드시 다음 둘 중 하나여야 한다.

1. **Agent 안의 Tool 호출 횟수를 줄인다**
2. **컨텍스트에 들어오는 원문 자체를 줄인다**

**Agent를 더 쪼개는 것은 둘 중 어느 것도 하지 못한다.** 이것이 stage 축 분할이 4회 실패한 이유다.

---

## 3. 기각된 방향

| 방향 | 시도 이력 | 기각 사유 |
|---|---|---|
| 예산 증액 | 100k → 945k (모델 전환) | 사용량이 더 크게 증가. 1.1절 |
| Tool 분산(Agent 3분할) | 1차 실패 후 적용 | 같은 Tool의 페이지 반복은 그대로. 2차 재발 |
| `pageSize` 상향 | 10 → 200 + 반복상한 3회 | 반복 횟수만 줄이는 미봉책. 3차 재발 |
| 담당자 단위 스코프 축소(#1B-2) | 3차 대응 | 담당자 1명이 수백 건 보유 시 가정 붕괴 |
| 프롬프트 요약 지시 | 경량 스캔 | 규칙 ② — 사후 요약은 절감 없음 |
| 데이터 강제 축소 | 담당자 1명/30건/5건 | 기능 검증 전용. 운영 구성 아님 |

> 📌 **`asstLCtgrId` 추가·`pageSize` 조정 같은 파라미터 튜닝도 모두 기각됐다.** 개발기 curl 진단(`scanresult-debug.sh`) 결과 `pageSize`는 무관(1순위 가설 기각), `asstLCtgrId`도 불필요(2순위 가설 기각)로 확정됐다.

---

## 4. 설계 전 선행 측정 (반나절)

분해 단위를 숫자로 정하려면 "호출 1회의 비용"과 "증가 곡선"을 알아야 한다. **현재는 이 값을 아무도 모르는 상태에서 30건/5건 같은 상한을 감으로 잡고 있으며, 지금까지 감으로 잡은 상한은 네 번 모두 틀렸다.**

### 4.1 교정 플로우

```
Chat Input → Agent (Tool 1개) → Chat Output      (4노드)
```

System Prompt로 호출 횟수만 `1 → 2 → 4 → 8`회로 강제하고 어디서 초과하는지 관측한다.

### 4.2 측정 항목

| 확인할 것 | 판단 기준 |
|---|---|
| 호출 1회당 추정 토큰 | 배치 크기 산정의 기준선 |
| 1→2→4회 증가폭 | **선형이면 규칙 ① 기각** → 분해 여지가 훨씬 큼<br>**제곱이면 규칙 ① 확정** → Agent당 호출 1회 원칙 필수 |
| 통과하는 최대 호출 횟수 | 워크플로우 1개의 처리 단위 상한 |

### 4.3 측정 대상 Tool 2종

| Tool | Method | 측정 목적 |
|---|---|---|
| `orgList` | GET | 응답이 작을 때의 **호출 횟수 요인** 분리 |
| `mngtListDetail` (pageSize 200) | POST | 응답이 클 때의 **응답 크기 요인** 분리 |

두 개를 각각 재면 호출 횟수 요인과 응답 크기 요인이 분리된다.

> 🔴 **이 측정 없이 분해안을 확정하면 또 감으로 잡는 것이다.**

---

## 4-1. 실측 결과 (2026-08-20, 개발기)

4절의 교정 측정(캔버스 필요)과 별개로, **API 응답 volume은 curl로 즉시 측정 가능**하므로 먼저 실행했다.

측정 환경: 개발기 `http://165.244.21.49:8080`, `org_000991` / `templateNo=151` / `diagYear=2026`

### 4-1.1 🔴 서버 측 필터는 동작하지 않는다 — 8.2절 기각

`filter.xorStr` 유무·조건을 바꿔 3회 호출한 결과가 **MD5 해시까지 완전히 동일**했다.

| 호출 | listCount | bytes | 해시 |
|---|---|---|---|
| 필터 없음 | 9,794 | 343,477 | `1b46aa4c…` |
| `SECURITY_SCORE neq 100` (미조치만) | 9,794 | 343,477 | `1b46aa4c…` |
| `SECURITY_SCORE eq 100` (조치완료만) | 9,794 | 343,477 | `1b46aa4c…` |

응답 200건 중 `securityScore=100`인 자산은 2건뿐인데 `eq 100` 조건에서도 200건이 그대로 반환됐다. **서버가 `filter` 객체를 무시한다.**

> **판정: 방식 B는 "미검증"이 아니라 "무효"다.** `11-flow-a-node-detail-config.md` 2-3절과 12번 문서 8.2절의 "검증 필요" 상태를 **기각으로 확정**한다.
>
> 결과적으로 **규칙 ③(projection 지점 부재)을 완화할 수단이 완전히 사라졌다.** 응답 원문을 줄이는 유일한 서버 측 희망이었다.

### 4-1.2 스코프 파라미터 — `mgmtOrgId`는 유효, `rspnMngId`는 0건

| 호출 | listCount | 판정 |
|---|---|---|
| `mgmtOrgId` 미지정 | 47,346 | 전체 시스템 |
| `mgmtOrgId=org_000991` | **9,794** | ✅ 유효 |
| `mgmtOrgId=org_002205`(상위) | 11,263 | ✅ 유효 |
| `+ rspnMngId=yoongeunkwon` | **0** | ⚠️ 0건 반환 |

**`mgmtOrgId`는 정상 동작한다.** 조직 단위 슬라이스는 유효하다.

`rspnMngId`는 0건을 반환했는데, 원인은 4-1.4절에서 밝혀진다.

> 🔴 **2026-08-21 정정 — 위 9,794건은 실자산이 아니다.** 가짜 조직 ID 대조를 하지 않아 판단이 불완전했다. 실제 조직 자산은 **약 112건**이며, 나머지 9,682건은 조직 ID가 미기입된 개발기 자산이 조건과 무관하게 딸려온 것이다. 상세는 **4-2절**을 따르며, 본 절의 건수를 규모 산정에 사용하지 말 것.

### 4-1.3 🔴 규칙 ③의 정량화 — 레코드당 9.1배 낭비

| 항목 | 값 |
|---|---|
| 레코드 필드 수 | **70개** |
| 레코드 전체 크기 | 1,466 bytes |
| 계층 2가 실제로 쓰는 7개 필드 | **161 bytes** |
| **낭비 배율** | **9.1배** |

`asstCode`/`chrgId`/`chrgNm`/`securityScore`/`timeEndYmd`/`asstNm`/`hostNm` 7개만 필요한데 `asstConf`·`asstIntg`·`networkRealmCd`·`installDirectory` 등 63개 필드가 함께 온다.

**캔버스에는 이를 잘라낼 노드가 없으므로 9.1배 낭비를 그대로 컨텍스트에 싣는다.** 이것이 규칙 ③의 실체다.

### 4-1.4 🔴 개발기에는 담당자 데이터가 없다

7개 페이지(1/8/16/24/32/40/49) 1,394건을 샘플링했다.

| page | recs | chrgId 있음 | 진단결과 있음 | 미조치(≠100) | bytes |
|---|---|---|---|---|---|
| 1 | 200 | **0** | 68 | 66 | 343,477 |
| 8 | 200 | **0** | 0 | 0 | 315,944 |
| 16 | 200 | **0** | 128 | 128 | 331,873 |
| 24 | 200 | **0** | 84 | 84 | 584,352 |
| 32 | 200 | **0** | 101 | 100 | 329,591 |
| 40 | 200 | **0** | 52 | 52 | 322,202 |
| 49 | 194 | **0** | 58 | 58 | 315,361 |
| **계** | **1,394** | **0** | 491 | 488 | — |

**담당자(`chrgId`)가 전 샘플에서 0건이다.** `chrgNm`/`subChrgId`/`chrgOrgIdStr` 등 담당자 관련 8개 필드가 모두 빈 문자열이다.

이것이 4-1.2절 `rspnMngId=0건`의 원인이며, 다음을 의미한다.

- **담당자 기반 설계(#1B 경량 스캔 → #1B-2 `rspnMngId` 정밀조회)는 개발기에서 검증할 수 없다**
- 2·2-2단계에서 확인된 담당자 2명(`yoongeunkwon`, `kiwook80`)은 **운영기 데이터**였다
- 운영기는 현재 네트워크에서 도달 불가(30초 타임아웃) → **환경 차이를 문서화하고, 담당자 관련 검증은 운영기 접근 가능한 망에서 수행해야 한다**

미조치 비율은 **35%**(488/1,394)로, 전량 추정 **약 3,428건**이다.

### 4-1.5 예산 대비 환산 — 캔버스 내 전량 처리는 불가

페이지 1회 평균 363,257 bytes ≈ **121,085 토큰**(bytes/3 환산).

| 항목 | 값 | 예산(945,000) 대비 |
|---|---|---|
| 호출 1회 | 121,085 tok | **13%** |
| 누적 없이 가능한 호출 수 | **7회** | — |
| 규칙 ①(제곱 누적) 적용 시 | **3회** | — |
| 전량 49페이지 | 5,933,165 tok | **6.3배 초과** |

> 🔑 **규칙 ①의 간접 검증**: 제곱 누적 가정 시 상한이 3회로 계산되는데, 이는 관측된 실패와 정합한다. `pageSize=200` 반복상한 3회(600건) 설정에서 터졌고 그때 추정치가 1,324,532였다. 4회 호출의 제곱 누적은 `10 × 121,085 = 1,210,850`으로 **관측값 1,324,532에 근접**한다.
>
> 선형 누적이라면 4회는 484,340으로 예산 내에 들어와야 했으므로, **규칙 ①(제곱 누적)이 실측과 더 잘 맞는다.** 4절 교정 측정으로 확정할 것.

### 4-1.6 이 측정이 계획을 바꾸는 지점

> ⚠️ **아래 규모 관련 항목은 4-2절에서 뒤집혔다.** 9,794건은 실자산이 아니었다. 규모·실행 횟수는 **4-2.5절**을 따를 것.

| 기존 계획 | 실측 후 |
|---|---|
| 8.2절 서버 필터 검증 → 성공 시 배치 상향 | **기각.** 규칙 ③ 완화 수단 소멸 |
| 계층 2 슬라이스 축 = 담당자(`rspnMngId`) | **개발기 검증 불가.** `asstCode` 배치 축으로 전환 |
| ~~규모 추정 1,600건~~ | ~~실측 9,794건~~ → **4-2절 정정: 실자산 약 112건** |
| ~~대상 조직 전량 처리 불가~~ | ~~예산의 6.3배~~ → **4-2절 정정: 12.8%, 1회 완주** |
| ~~계층 1에 17회 실행 필요~~ | ~~현 범위로는 불가~~ → **4-2절 정정: 1회** |

**projection이 가능하다면** 9.1배 절감으로 전량이 `5,933,165 ÷ 9.1 ≈ 652,000` 토큰이 되어 예산 내에 들어온다. 그러나 캔버스에 projection 지점이 없고(규칙 ③) 외부 수단은 범위 밖이므로, **이 수치는 REQ-004 등 플랫폼 개선 요구의 정량 근거로만 쓴다.**

역으로 계산하면 **범위 축소의 목표치**가 나온다. projection 없이 예산에 들어오려면 처리 대상이 **7페이지(약 1,400건) 이하**여야 하고, 규칙 ①(제곱 누적)을 적용하면 **Agent 1개당 3페이지(600건)**가 상한이다.

---

## 4-2. 조직 규모 재측정 — 9,794건의 정체 (2026-08-21, 개발기)

4-1절이 남긴 "전량 6.3배 초과"를 조직 단위 축소로 해결할 수 있는지 재려다가, **4-1.2절 판정 자체가 틀렸음**을 발견했다. 스크립트: `ixi-enterprise/orgscale-debug.sh` (재현 가능)

### 4-2.1 🔴 가짜 조직 ID 대조 — 9,794건의 98.8%는 조직과 무관하다

존재하지 않는 조직 ID로 조회해도 **9,682건**이 반환된다.

| `mgmtOrgId` | listCount | 표본 200건의 조직 분포 |
|---|---|---|
| 미지정 | 47,346 | 타조직 199 / 빈값 1 |
| `org_000991`(실재) | 9,794 | **일치 112** / 빈값 88 |
| `org_ZZZZZZZ`(가짜) | **9,682** | **빈값 200** (전량) |
| `NOT_AN_ORG`(가짜) | **9,682** | — |

가짜 ID 응답이 **전량 조직ID 빈값**이라는 것이 판정의 핵심이다. 이는 필터가 무효라는 뜻이 아니라, **조직 ID가 미기입된 자산이 조건과 무관하게 항상 딸려온다**는 뜻이다.

> ✅ **`mgmtOrgId` 필터 자체는 정상 동작한다.** 실자산은 다음과 같이 보정한다.
>
> **실자산 ≈ (조직 조회 건수) − (가짜 ID 건수)**

동일 조직 3회 반복 조회에서 9,794건이 안정적으로 재현되므로, 측정 오차가 아니다.

### 4-2.2 garbage 구간의 위치

무필터 전량 47,346건(237페이지)을 이분 탐색한 결과, 조직ID 빈값 자산은 **뒤쪽에 연속으로 몰려 있다.**

| 구간 | 규모 | `mgmtOrgId` |
|---|---|---|
| page 1~188 | 약 **37,600건 (79%)** | ✅ 정상 채워짐 |
| page 189~237 | 약 **9,700건 (21%)** | ❌ 전량 빈 값 |

**무필터 조회 시 빈값은 200건 중 1건(0.5%)뿐이다.** 개발기 데이터가 전반적으로 부실한 것이 아니라, 뒤쪽 21%만 조직 미기입 상태다.

### 4-2.3 🔵 조직별 실자산 — 전부 예산 안에 들어온다

| 조직ID | 조직명 | 반환건수 | **실자산** | 페이지 |
|---|---|---|---|---|
| `org_002205` | SW개발센터(상위) | 11,263 | **1,581** | 8 |
| `org_000991` | Enterprise SW프로덕트개발팀 | 9,794 | **112** | 1 |
| `org_002553` | AX엔지니어링Lab | 9,735 | **53** | 1 |
| `org_002201` | AICC개발Lab | 9,751 | **69** | 1 |
| `org_001738` | Enterprise서비스개발Lab | 10,157 | **475** | 3 |
| `org_002485` | ixi Agent개발lab | 9,712 | **30** | 1 |
| `org_001417` | Consumer서비스개발Lab | 10,636 | **954** | 5 |

조직 트리상 `org_000991`은 **최하위**다(하위 조회 시 자기 자신만 반환). 형제 조직 5개는 상위 `org_002205` 아래에 있다.

### 4-2.4 예산 환산 — 캔버스 단독 성립

| 기준 | 건수 | 페이지 | 추정 토큰 | 예산 대비 |
|---|---|---|---|---|
| 개발기 반환값(garbage 포함) | 9,794 | 49 | 5,933,165 | **627.8%** |
| **실자산(garbage 제외)** | **112** | **1** | **121,085** | **12.8%** |

| 누적 가정 | Agent당 상한 | 계층 1 실행 횟수 | 판정 |
|---|---|---|---|
| 규칙 ①(제곱 누적) | 3페이지(600건) | **1회** | ✅ 성립 |
| 선형 | 7페이지(1,400건) | **1회** | ✅ 성립 |

**최대 조직(`org_002205` 1,581건)조차 7페이지 임계에 근접할 뿐이며, 하위 조직 단위로는 전부 1~5페이지다.**

### 4-2.5 이 측정이 계획을 바꾸는 지점

| 4-1절 결론 | 4-2절 정정 |
|---|---|
| 대상 조직 자산 9,794건 | **약 112건.** 9,682건은 조직 미기입 garbage |
| 전량이 예산의 6.3배 | **12.8%.** 1회 호출로 완주 |
| 계층 1에 17회 수동 실행 필요 | **1회** |
| `mgmtOrgId` 유효 (근거 불충분) | **유효 확정** (가짜 ID 대조로 검증) |
| 캔버스 단독 운영 불가 | **조직 단위로는 성립** — 단 운영기 규모 확인 필요 |

### 4-2.6 ⚠️ 남는 제약 두 가지

**1. 개발기에서는 garbage를 걸러낼 수 없다.** 서버 필터가 무효이고(4-1.1절) 캔버스에 projection 노드가 없으므로(규칙 ③), 개발기에서 조직 조회를 하면 9,682건이 **무조건 컨텍스트에 실린다**. 즉 실자산이 112건이어도 캔버스 실행은 여전히 예산을 초과한다. **개발기에서 가능한 것은 `asstCode` 직접 지정 방식의 기능 검증까지**다.

**2. 운영기 실규모는 미측정이다.** 개발기 112건이 운영기에서 몇 건일지 알 수 없다. `chrgId` 채움률 0%(4-1.4절)와 같은 성격의 환경 차이이며, 운영기 접근 가능한 망에서 본 스크립트를 재실행해야 확정된다.

---

## 4-3. 규정항목 조회 경로 실측 (2026-08-21, 개발기)

W0-0(참조값 후보 조회) 작업 중 "`templateNo`별 하위 기준항목을 조회할 수 있는가"를 확인하면서 측정했다. 결과가 W2 설계에 영향을 준다.

### 4-3.1 ❌ `templateNo`로 기준항목을 조회하는 경로는 없다

`08-ivms_openapi_spec.md` 4.6절의 `guidelineCdList`(규정항목 목록 조회)가 후보였으나, 받는 키가 다르다.

| 요청 | 결과 |
|---|---|
| `templateNo=151` | ❌ `msgCd: E` |
| `atemplateNo=151` | ❌ `msgCd: E` |
| 빈 body | ❌ `msgCd: E` |
| **`resultId="RESULT_01493643"`** | ✅ 29건 반환 |

`resultId`는 **자산별 진단결과 ID**다(`mngtListDetail` 응답의 `resultId` 필드에서 획득). 즉 이 API도 `guidelineCdInfo`와 같은 **진단결과 종속**이며, 반환값은 템플릿 정의가 아니라 해당 자산이 실제로 진단받은 결과다.

> **판정: 템플릿별 기준항목 조회는 현재 API 집합으로 불가능하다.** 필요하다면 IVMS 운영팀에 별도 문의해야 한다(관리자 화면에는 해당 기능이 있을 가능성이 있으나 스펙 문서에 없다).

### 4-3.2 🔴 스펙표 불일치 3번째 — `resultId`는 배열이 아니다

스펙표는 `resultId`를 `Array[string]`으로 기재하나 **실제로는 문자열**이어야 한다.

| 전달 형태 | 결과 |
|---|---|
| `"resultId": "RESULT_01493643"` | ✅ 정상 |
| `"resultId": ["RESULT_01493643"]` | ❌ **JSON이 아닌 HTML 에러 페이지 반환** |
| `"resultId": [...]` + `guidelineCd` 동봉 | ❌ HTML |
| `"resultId": [...]` + `userId` 동봉 | ❌ HTML |

`mngtListDetail`·`scanResultCodeMngtDetail`에 이어 **세 번째 스펙표 불일치**다. 8.4절의 "IVMS 스펙표 필수 Y/N 불신" 원칙에 **타입 표기도 불신 대상**으로 추가한다.

> ⚠️ 실패 시 `msgCd: E`가 아니라 **HTML을 반환**하는 것도 새로운 실패 모드다. Agent가 HTML을 응답으로 받으면 파싱에 실패하거나 엉뚱한 내용을 읽을 수 있다.

### 4-3.3 🔵 W2에 쓸모가 있다 — 단 응답이 크다

`guidelineCdList`는 **한 자산의 전 항목을 1회 호출로** 가져온다. 항목당 1회가 필요한 `guidelineCdInfo`(6.2절)와 대조된다.

| 항목 | 값 |
|---|---|
| 반환 항목 수 | 29건 (MariaDB 자산 1건 기준) |
| 응답 크기 | **181,465자 ≈ 60,488 tok** |
| 예산(945,000) 대비 | **6.4%** — 자산 1건에 |
| 필드 | `guidelineCd`/`category`/`subjectType`/`severity`/`subjectSubType`/`guidelineNm`/`criteria`/`analysisInfo`/`measure`/`measureDetailOrigin` |

항목 1건이 약 6KB다. `criteria`·`analysisInfo`·`measure`·`measureDetailOrigin` 4개 필드가 각각 수백 자씩이기 때문이다.

**W2 설계 시 트레이드오프**

| 방식 | 호출 횟수 | 응답 크기 | 규칙 ① 영향 |
|---|---|---|---|
| `guidelineCdInfo` (현재 설계) | **1 + 취약항목 수** | 항목당 소량 | 제곱 누적 발생 |
| `guidelineCdList` | **자산당 1회** | **자산당 약 60,000 tok** | 호출 수는 적으나 원문이 큼 |

> 🔴 **어느 쪽도 무료가 아니다.** `guidelineCdList`로 바꾸면 호출 횟수는 줄지만 자산 3건이면 이미 예산의 19%다. 배치 크기를 **자산 수 기준으로 재산정**해야 하며, 정확한 값은 9절 5번(기능 시범)에서 3단계 출력의 실제 항목 수를 보고 정한다.

---

## 5. 분해 축 전환 — stage에서 data slice로

### 5.1 지금까지의 축: stage (기각)

```
#1A(조직) → #1B(목록) → #1B-2(정밀) → #1C(취약점)
```

**어느 API를 쓰느냐**로 갈랐다. 규칙 ①을 건드리지 못한다 — 각 Agent가 여전히 자기 안에서 페이지를 반복한다.

### 5.2 바꿀 축: data slice (채택)

**한 실행이 처리하는 데이터 범위**를 잘라, 어떤 Agent도 Tool을 1~2회만 호출하고 끝나게 만든다.

아울러 4차 실패의 교훈("파이프라인이 길어지면 앞단 비용이 뒤로 전파된다")에 따라 **4-Agent 체인 자체를 해체**한다.

> **원칙: 워크플로우 하나에 Agent 하나, Agent 하나에 Tool 호출 1~2회.**

---

## 6. 재구성안 — 3계층 분해

### 6.1 전체 구조

```
[W0-1] 조직 조회      orgList 재귀           → orgId/templateNo/asstType   (조직 변경 시만)
[W0-2] 가이드 적재    ❌ 기각 — guidelineCdInfo가 aresultNo 종속 (6.2절 실측)
──────────────────────────── 이하 런타임 ────────────────────────────
[W1]   목록 산출      mngtListDetail        → asstCode·chrgId 목록         (조직당 1회)
[W2]   배치 처리      scanResultCodeMngtDetail 1회 + guidelineCdInfo
                                            → 담당자별 안내문              (배치당 1회 × N)
```

계층 간 전달은 **사람이 W1 출력을 W2의 Chat Input에 붙여넣는 방식**이다. Sub-flow 노드가 없으므로(REQ-008) 자동 연결이 불가능하다.

### 6.2 계층 0 — 정적 참조 데이터를 런타임에서 제거

**가장 큰 즉효.** 매 실행마다 재조회할 필요가 전혀 없는 값들이 런타임 체인에 박혀 있다.

| 대상 | 현재 | 변경 |
|---|---|---|
| `orgId` / `templateNo` / `asstType` | #1A가 `orgList` 재귀 호출 → 체인 앞단에서 컨텍스트를 뒤로 전파 | **별도 플로우(W0-1)로 분리.** 조직 변경 시에만 실행, 결과는 상수로 관리 |
| `guidelineCd` → `measure` | 4단계에서 항목마다 API 호출 | **사전 전량 적재 → 정적 사전.** 런타임 체인에서 제거 |

#### 🔵 4차 실패의 원인 재귀속 (2026-08-21 실측)

12번 문서 초판과 관련 문서 다수가 "**`orgList` 재귀 6회가 4차 실패(1,017,179 tok)의 직접 원인**"으로 서술했으나, **실측 결과 이 귀속은 과도하다.**

| 항목 | 실측값 |
|---|---|
| `orgList` 1회 응답 | 69~710 bytes (**23~236 tok**) |
| 재귀 7회 전체 누적 원문 | 2,122 bytes (**약 707 tok**) |
| 규칙 ①(제곱 누적) 적용 시 | 약 19,796 tok |
| **관측된 실패치** | **1,017,179 tok** |

**`orgList`가 기여한 몫은 전체의 2%에 못 미친다.** 계층 0 Tool 3종을 전부 합쳐도 약 2,100 tok에 불과하다(`assetSsrcceTemplate` 736 + `assetCategory` 1,012 + `orgList` 707 상당).

원본 기록(`stage2_2-3-test-guide.md` 1.2-4절)은 실제로 정확한 원인을 이미 적어두었다.

> "차이는 **파이프라인 길이**다. #1A → #1B → #1B-2로 이어지며 앞 단계 컨텍스트가 뒤로 계속 전달된다."

즉 1,017,179는 `orgList` 응답 크기가 아니라 **4-Agent 체인 전체가 누적 전달한 컨텍스트 총량**이며, #1A는 그 총량이 임계를 넘은 **지점**이지 원인의 대부분이 아니다. 1·2단계에서 동일한 `orgList` 재귀가 통과했다는 사실이 이를 뒷받침한다.

> 🔴 **따라서 계층 0 분리의 진짜 근거는 "`orgList`가 비싸서"가 아니라 "체인이 길어서"다.** W0-1 분리로 얻는 것은 토큰 절감(약 2천 tok, 미미)이 아니라 **체인 단절**이다. 5절의 "4-Agent 체인 해체" 원칙이 본질이고, 계층 0 분리는 그 첫 단계다.

#### 🔴 `#1A 하드코딩 되돌리기`는 취소한다

현재 TODO(`stage5-test-guide.md` 6절, `11-flow-a-node-detail-config.md`)에는 "**#1A 하드코딩은 기능 검증 전용이며 반드시 되돌려야 한다**"로 기록되어 있으나, **이 항목은 철회한다.**

되돌릴 것이 아니라 **별도 워크플로우로 떼어내는 것**이 맞다. 조직 ID는 하루에 한 번도 바뀌지 않는 값인데 실행마다 재탐색할 이유가 없고, 무엇보다 **그 재탐색이 런타임 체인에 있으면 뒤 단계로 컨텍스트가 전파된다.**

#### 🔴 W0-2 조치가이드 사전 적재 — 기각 (2026-08-21 실측)

초판은 `guidelineCd`별 `measure`를 자산·담당자와 무관한 **고정 사전**으로 보고, 사전 전량 적재로 4단계 API를 런타임 체인에서 제거할 수 있다고 판단했다. **실측 결과 이 전제가 틀렸다.**

`guidelineCdInfo`는 **4개 키의 정확한 조합**을 요구하며, 그중 `aresultNo`(진단결과 번호)는 **자산별 진단 결과에 종속된 값**이다.

| 변형 | 결과 |
|---|---|
| 문서 검증 조합 그대로 | ✅ `measure` 569자 |
| `aresultNo` 생략 | ❌ `msgCd: E` |
| `aresultNo`를 다른 값으로(269954 / 270000 / 1) | ⚠️ **200 응답이나 전 필드 빈 값** |
| `guidelineCd`만 변경(DBM-002 / U-103) | ⚠️ **200 응답이나 전 필드 빈 값** |
| `itemCode` 변경 | ⚠️ 빈 값 |
| `agentServerNm` 변경 | ⚠️ 빈 값 |
| `guidelineIfKey` 변경 | ✅ 정상 (이 키만 무관) |

> 🔴 **결론: 조치가이드는 "코드로 조회 가능한 사전"이 아니라 진단결과에 종속된 조회다.** `guidelineCd` 목록만으로 순회 수집할 수 없으므로 사전 전량 적재가 성립하지 않는다. **3단계(`scanResultCodeMngtDetail`) 출력이 4단계의 입력 전제**이며, 이 의존은 끊을 수 없다.
>
> ⚠️ **실패 모드 주의**: 키가 어긋나면 오류가 아니라 **빈 값을 200으로 반환**한다. Agent가 이를 "조회 성공, 내용 없음"으로 오인해 빈 안내문을 생성할 수 있다. 4단계 프롬프트에 "`measure`가 비어 있으면 실패로 간주하고 그 사실을 명시하라"를 반드시 포함할 것.

**따라서 8.3절(KMS 정확 일치 조회 적합성)은 검토할 필요가 없어졌다** — 적재할 사전 자체를 만들 수 없다.

| 항목 | 초판 | 실측 후 |
|---|---|---|
| W0-2 가이드 사전 적재 | 계층 0으로 분리 | ❌ **기각** — 4단계는 런타임 체인에 남는다 |
| 체인 축소 효과 | 5단계 → 3단계 | **5단계 → 4단계** (W0-1 분리분만) |

#### 계층 0의 효과 (수정)

W0-1 분리로 체인이 **5단계 → 4단계**로 축소된다. W0-2가 기각됐으므로 4단계 API는 런타임에 남는다.

단 6.2절 첫머리에서 밝혔듯 **분리의 실익은 토큰 절감이 아니라 체인 단절**이다. 계층 0 Tool 3종의 응답은 합쳐야 약 2,100 tok에 불과하다.

### 6.3 계층 1 — 목록 산출 (조직당 1회)

| 항목 | 내용 |
|---|---|
| 구성 | Chat Input → Agent(Tool: `mngtListDetail`) → Chat Output |
| 입력 | W0-1이 확정한 `orgId`/`templateNo`/`asstType` (상수) |
| 산출물 | `asstCode` + `chrgId` 목록 **only** |
| 호출 | `pageSize=200`으로 최소화 |

**이 플로우는 자산 상세를 하나도 보유하지 않아도 된다.** `asstCode`와 `chrgId`만 나오면 계층 2가 동작한다.

산출물은 계층 2의 **실행 파라미터 목록**이 된다.

> ⚠️ **신규 리스크 — 출력 토큰 상한**: 800건 규모면 Agent가 800줄을 출력해야 하며, 모델 출력 상한에 걸릴 수 있다. 이는 **지금까지 한 번도 부딪히지 않은 제약**이다(항상 입력 쪽에서 먼저 초과됐으므로). 담당자 그룹 단위로 나눠 출력해야 할 가능성이 높다.

### 6.4 계층 2 — 배치 단위 처리 (N회 반복)

| 항목 | 내용 |
|---|---|
| 구성 | Chat Input → Agent(Tool: `scanResultCodeMngtDetail` + `guidelineCdInfo`) → Chat Output |
| 입력 | `asstCode` 배치(예: 30건) — 계층 1 출력에서 붙여넣기 |
| 호출 | `scanResultCodeMngtDetail` **1회** + `guidelineCdInfo` 항목당 1회 |
| 산출물 | 담당자별 안내문 |

`scanResultCodeMngtDetail`은 `asstCode`를 **배열로 수신**하므로 30건을 1회 호출로 처리할 수 있다(3단계 검증에서 확인된 방식).

> ⚠️ **W0-2 기각으로 조치가이드 조회가 이 계층에 남는다**(6.2절). 사전 적재가 불가능하므로 런타임에 호출해야 하며, **선택지가 2개이고 둘 다 비용이 있다**(4-3.3절).

#### 조치가이드 조회 방식 — 미결정

| 방식 | 키 | 호출 횟수 | 응답 크기 | 리스크 |
|---|---|---|---|---|
| **A** `guidelineCdInfo` | `aresultNo`+`guidelineCd`+`itemCode`+`agentServerNm` | **1 + 취약항목 수** | 항목당 소량 | 규칙 ① 제곱 누적 |
| **B** `guidelineCdList` | `resultId` (문자열) | **자산당 1회** | **자산당 약 60,488 tok** | 자산 3건 = 예산 19% |

방식 A는 호출이 많아 제곱 누적에 걸리고, 방식 B는 호출은 1회지만 원문이 크다. **어느 쪽도 자산 30건 배치를 감당하지 못한다.**

> 🔴 **배치 크기는 3단계 출력의 실제 취약항목 수를 본 뒤 정한다**(9절 5번 기능 시범). 자산 30건은 현재 근거가 없는 값이며, 방식 B 기준으로는 **자산 3~5건**이 상한일 가능성이 높다.
>
> 두 API 모두 **실패 시 오류를 주지 않는 것**에 주의할 것. A는 빈 값을 200으로(6.2절), B는 HTML을 반환한다(4-3.2절). 프롬프트에 "내용이 비어 있거나 형식이 다르면 실패로 간주하고 명시하라"를 반드시 포함한다.

#### 확정 파라미터 (개발기 curl 진단 결과 반영)

| 파라미터 | 값 | 근거 |
|---|---|---|
| `hostNm` | **전달 금지** | `asstCode`와 AND로 걸려 결과가 0건이 됨 (`stage2_2-3-test-guide.md` 2.2-2절) |
| `vadaYn` | `"N"` (실제 필수) | 스펙표는 선택(N)이나 생략 시 `msgCd: E` |
| `resultStatusCdListStr` | `"[\"FAIL\"]"` (실제 필수) | 동일 |
| `severity` | `"4"` | 스펙표 필수(Y) |
| `pageSize` | `50` | 값 제한 없음이 확인됐으나 보수적 유지 |

### 6.5 계층별 요약

| 워크플로우 | 노드 수 | Agent | Tool 호출 | 실행 빈도 | 상태 |
|---|---|---|---|---|---|
| W0-0 참조값 후보 조회 | 6 | 1 | 1~3회 | 값 확정 시 1회 | ✅ **JSON 완료** |
| W0-1 조직 조회 | **6** | 1 | 최대 4회 (격리됨) | 조직 변경 시 | ✅ **운영기 검증 완료** |
| ~~W0-2 가이드 적재~~ | — | — | — | — | ❌ **기각** (6.2절) |
| W1 목록 산출 | 4~5 | 1 | 1~3회 | 조직당 1회 | ⏳ |
| W2 배치 처리 | 5~6 | 1 | 방식 미결정 (6.4절) | 배치당 1회 × N | ⏳ |

#### W0-1 — ✅ 운영기 실행 검증 완료 (2026-08-21)

**산출물**: `ixi-enterprise/w01-org-lookup.json` (생성기 `build-w01.py`, 재현 가능)

| 항목 | 내용 |
|---|---|
| 구성 | Chat Input → Agent(Tool 3종) → Chat Output — 6노드 |
| Tool | `orgList` / `assetSsrcceTemplate` / `assetCategory` (전부 GET) |
| 입력 | `Enterprise SW프로덕트개발팀 참조값 확인해줘` |
| **산출값** | `org_000991` / Enterprise SW프로덕트개발팀 / `151` / `SSRCCE` |
| **Tool 호출** | **4회** (orgList 2 + 템플릿 1 + 자산분류 1) — 상한 4회 내 |
| 컨텍스트 | 초과 없음. 추정 약 2,100 tok + 프롬프트 (예산의 1% 미만) |

**확인된 사실 3가지**

1. **운영기와 개발기의 참조값이 동일하다** — 산출값 4개 전부 curl 선확인값과 일치. 조직 트리·템플릿·자산분류에서는 환경 차이가 없다
2. **인증 헤더 4종 빈 값이 운영기에서도 통과한다** — 개발기에서만 확인됐던 사실(2026-08-18)이 운영기로 확장됐다
3. **체인 단절이 실효를 냈다** — stage1에서 6회까지 갔던 `orgList` 호출이 2회로 끝났고, 4차 실패의 재발이 없었다. 6.2절의 원인 재귀속(체인 길이가 문제)이 실행으로도 뒷받침된다

**확정된 상수** — W1/W2에 하드코딩할 값

```
orgId      = org_000991
templateNo = 151
asstType   = SSRCCE
diagYear   = 2026
```

> ⚠️ `templateNo=151`은 프롬프트의 "배열 첫 번째 항목" 규칙으로 선택된 값이다. 후보가 26개이고 `(구)`·`_bak`·`_복제본`·`테스트` 항목이 섞여 있어 **API 반환 순서가 바뀌면 다른 값이 선택된다.** W0-0으로 목록을 확인한 뒤 **이름 기반 조회로 전환할 것**(`templateName`이 `LG유플러스_기준항목(신규)`인 항목).

#### W0-0 — 참조값 후보 조회 (신규)

**산출물**: `ixi-enterprise/w00-reference-list.json` (생성기 `build-w00.py`, 설명서 `w00-usage-guide.md`)

W0-1이 값을 1개로 좁히는 반면, W0-0은 **후보 전체를 나열**한다. 값을 정할 때 1회 실행하는 조회 전용 플로우다. W0-1에 목록 출력을 얹지 않고 분리한 이유는 26개 템플릿 전문이 컨텍스트와 출력에 실리기 때문이다.

| 조회 | Tool | 개발기 실측 |
|---|---|---|
| 진단템플릿 | `assetSsrcceTemplate` | 26건 |
| 자산분류 | `assetCategory` (`asstCtgrLevel=L`) | 8건 — **전부 `SSRCCE`** |
| 조직 | `orgList` | Lab 5개 / 팀 37개 |

> `asstType`은 8개 항목이 전부 동일해 **선택의 여지가 없고**, `orgId`는 조직명 정확 일치로 확정되므로 순서 의존이 없다. **확인이 필요한 것은 사실상 `templateNo` 하나다.**
>
> ⚠️ `asstCtgrLevel`은 `L`로 고정되어 있다. 실측상 `M`·`S`는 각 87건을 반환하나 현재 프롬프트는 조회하지 않는다.

---

## 7. 실행 횟수 — 조직 단위로 자르면 성립한다

### 7.1 실행 횟수 추정 (4-2절 재측정 반영)

> 🔵 **4-1절 기준의 초판 추정(계층 1 17회 / 계층 2 114회 / 조직 1개 130회 이상)은 폐기한다.** 그 산술의 분자였던 9,794건이 실자산이 아니었다(4-2.1절).

Schedule/Webhook Trigger가 없으므로(REQ-007) 전부 Chat Input에 수동 입력해야 한다.

**계층 1 (목록 산출)** — Agent당 3페이지 상한(4-1.5절)

| 조직 | 실자산 | 페이지 | 실행 횟수 |
|---|---|---|---|
| `org_000991` | 112 | 1 | **1회** |
| `org_002485` | 30 | 1 | **1회** |
| `org_001738` | 475 | 3 | **1회** |
| `org_001417` | 954 | 5 | **2회** |
| `org_002205`(상위) | 1,581 | 8 | **3회** |

**계층 2 (배치 처리)** — 미조치 비율 35%(4-1.4절), 배치 30건

| 조직 | 미조치 추정 | 실행 횟수 |
|---|---|---|
| `org_000991` | 약 39 | **2회** |
| `org_001738` | 약 166 | **6회** |
| `org_001417` | 약 334 | **12회** |

| 용도 | 실행 횟수 | 판정 |
|---|---|---|
| 소규모 조직 1개 전량 | **3~4회** | ✅ 실용적 |
| 중규모 조직 1개 전량 | **8~14회** | ✅ 실용 범위 |
| 상위 조직(1,581건) 전량 | **약 30회** | ⚠️ 부담되나 가능 |
| 다수 조직 정기 운영 | 조직 수 × 위 값 | ⚠️ REQ-007 해결 시 완화 |

> ✅ **초판의 "조직 1개 전량 불가" 판정을 철회한다.** 조직 단위로 자르면 대부분의 조직이 **10회 안팎의 수동 실행**으로 완주한다.
>
> 다만 이는 **운영기에 garbage가 없다는 전제** 위에 있다. 개발기에서는 조직 조회 시 9,682건이 무조건 딸려오므로 위 수치가 성립하지 않는다(4-2.6절).

### 7.2 채택 방향 — `mgmtOrgId` 조직 단위 조회

> 🔵 **전제 (2026-08-21 확정)**: 해결은 ixi-enterprise 컴포넌트만으로 이루어져야 한다. 외부 집계 서버·배치 스크립트·MCP 서버 신설은 **범위 밖**이다.

4-2절에서 `mgmtOrgId` 필터가 **정상 동작함이 가짜 ID 대조로 확정**됐고, 조직별 실자산이 전부 예산 안에 들어옴이 확인됐다. 따라서 6절의 3계층 구조(W0/W1/W2)를 **조직 단위로 실행**하면 성립한다.

| 항목 | 값 | 근거 |
|---|---|---|
| 슬라이스 축 | `mgmtOrgId` (조직 1개 = 실행 1건) | 4-2.1절 |
| 조직당 상한 | 1,400건(7페이지) 이하면 계층 1이 1~3회 | 4-1.5절 역산 |
| 실측 조직 규모 | 30~1,581건 — **전부 상한 이내** | 4-2.3절 |
| 계층 2 배치 | `asstCode` 30건 단위, 호출 1회 | 6.4절 |

#### 🔴 선결 과제 — 운영기 실규모 측정

개발기 수치는 garbage 보정을 거친 **추정값**이며, 운영기 규모는 다를 수 있다. `ixi-enterprise/orgscale-debug.sh`의 `BASE_URL`을 운영기로 바꿔 재실행하면 확정된다.

| 측정 결과 | 판정 |
|---|---|
| 조직당 **1,400건 이하** | ✅ 본 계획 그대로 진행 |
| 조직당 **수천 건** | 조직 축으로 부족 → `asstCode` 배치 축 병용 또는 플랫폼 개선 선결 |

#### 남는 한계는 명시한다

범위를 줄여도 다음은 해소되지 않는다. 캔버스 컴포넌트로 메울 수 없는 항목이므로 **플랫폼 요구사항으로 제기한다**(`07-ixi-enterprise-requirements-spec.md`).

| 한계 | 관련 REQ |
|---|---|
| projection 지점 부재 → 레코드당 9.1배 낭비 | REQ-004 (🔴 High 상향) |
| Loop/Function 노드 부재 → 페이지 순회를 사람이 대신 | — |
| Sub-flow 부재 → 계층 간 전달이 수동 붙여넣기 | REQ-008 |
| Schedule/Webhook 부재 → 정기 실행 불가 | REQ-007 |

---

## 8. 검증이 필요한 미검증 사항

재구성안의 효율이 아래 항목들에 따라 크게 달라진다.

### 8.1 ~~API Request 일반 모드~~ → ✅ 검증 완료, 기각 (캔버스 실측)

초판은 `API Request(일반 모드) → Structured Output → JSON Output` 구성으로 **Agent를 거치지 않는 결정적 호출**이 가능하다면 계층 1이 LLM 없이 동작한다고 보아 이를 "최대 레버리지"로 지목했다.

**캔버스 실측 결과 일반 모드 자체가 존재하지 않는다.** `04-ixi-enterprise-node-catalog.md` 420~450행의 기재가 틀렸다.

| 항목 | 04번 카탈로그 기재 | 캔버스 실측 |
|---|---|---|
| `Tool Mode` 토글 | 상단 토글로 일반/Tool 모드 전환 | ❌ 부재 |
| `Data` 출력 포트(파란) | ✅ 확인(2026-05-18) | ❌ 부재 |
| `Data → JSON Output` 등 9종 연결 | ✅ UI 검증 완료 | ❌ 부재 |
| `Tool` 출력 포트(빨간) → Agent | ✅ 확인 | ✅ 실재 (**유일 경로**) |

> 🔴 **결론: 모든 IVMS API 호출은 Agent 경유가 필수이며, LLM 컨텍스트를 우회하는 경로는 캔버스에 존재하지 않는다.**
>
> 이로써 규칙 ③(projection 지점 부재)을 완화할 수단은 서버 측(4-1.1절 필터 기각)과 캔버스 측 모두에서 소멸했다.

#### REQ-004 우선순위 상향 근거 (🟡 Medium → 🔴 High)

`07-ixi-enterprise-requirements-spec.md` REQ-004(Tool 노드에 일반 모드 / Tool 모드 토글 추가)는 현재 🟡 Medium이다. 본 실측은 **REQ-004가 옳고 04번 카탈로그가 틀렸음**을 확인시킨다.

| 근거 | 내용 |
|---|---|
| ① 우회 경로 전무 | 일반 모드가 없으면 API 응답 원문이 **반드시** LLM 컨텍스트를 통과한다 |
| ② 정량 피해 확정 | 레코드당 9.1배 낭비(4-1.3절)가 전량 적재되어 전량 처리가 예산의 **6.3배** |
| ③ 캔버스 내 대체 불가 | Function/Loop 노드도 없어 캔버스 컴포넌트만으로는 메울 수 없다 |

> ⚠️ REQ-004가 구현되어도 **페이지네이션 순회·조인·미조치 선별은 여전히 불가**하다. Loop/Function 노드 부재는 별개 문제다.

### 8.2 ~~`mngtListDetail` 방식 B — 서버 측 필터링~~ → ✅ 검증 완료, 기각 (2026-08-20)

**결과: `filter.xorStr`은 서버에서 무시된다.** 필터 유무·조건을 바꾼 3회 호출의 응답이 MD5까지 동일했다. 상세는 4-1.1절.

| 항목 | 결과 |
|---|---|
| `filter.xorStr` 동작 | ❌ **무효** — 완전히 무시됨 |
| `mgmtOrgId` 동작 | ✅ **유효 확정** — 단 반환 건수의 98.8%는 조직 미기입 garbage. 실자산은 가짜 ID 대조로 보정할 것 (4-2.1절) |
| `rspnMngId` 동작 | ⚠️ 0건 — 개발기 담당자 데이터 부재가 원인(4-1.4절) |

**규칙 ③을 완화할 서버 측 수단은 존재하지 않음이 확정됐다.** 캔버스 측 수단도 8.1절에서 소멸했으므로, 응답 원문 자체를 줄이는 방법은 캔버스 범위 안에 없다. 남은 것은 **처리 범위 축소**(7.2절)뿐이다.

> `filter/xorStr`는 스펙상 `SECURITY_SCORE`, `AGENT_STATE`만 지원하고 날짜/기간 필터도 없는데, 그 둘조차 실제로는 동작하지 않는다.

**잔여 확인 사항** (운영기 접근 가능한 망에서):

| 항목 | 비고 |
|---|---|
| 운영기에서도 `filter`가 무시되는지 | 개발기만 미구현일 가능성 |
| 운영기 `rspnMngId` 동작 | 담당자 데이터가 있는 환경에서 재확인 |
| `severity` 필터 신뢰성 | `severity="4"` 요청에 `"5"`가 섞여 오는 이슈 기록됨 |

### 8.3 ~~KMS 지식베이스의 구조화 데이터 조회 적합성~~ → 검토 불요 (2026-08-21)

초판은 `guidelineCd` 정확 일치 조회를 KMS Retriever(RAG 유사도 검색)가 감당할 수 있는지를 리스크로 지목했다.

**이 검토는 불필요해졌다.** 6.2절 실측으로 `guidelineCdInfo`가 `aresultNo` 종속임이 확인되어 **적재할 사전 자체를 만들 수 없다.** KMS 적합성 이전의 문제다.

| 항목 | 판정 |
|---|---|
| 가이드 사전 전량 수집 | ❌ 불가 — `aresultNo`(진단결과별) 필요 |
| KMS 적재 후 Retriever 조회 | ❌ 검토 불요 — 적재 대상 부재 |
| 프롬프트에 상수 내장 | ❌ 불가 — 동일 사유 |
| **런타임 호출 유지** | ✅ **유일한 방법** — W2에 `guidelineCdInfo` Tool 유지 (6.4절) |

### 8.4 참고 — 이미 확정된 제약

재구성 시 반드시 준수할 항목들이다.

| 제약 | 내용 |
|---|---|
| System Prompt 중괄호 금지 | `{orgId}` 형태는 템플릿 변수로 해석되어 실행 거부. 서술형으로 표현. 단 `filter={"xorStr":{...}}` 형태의 JSON은 무방 |
| IVMS 스펙표 **필수 Y/N·타입 모두 불신** | `mngtListDetail`·`scanResultCodeMngtDetail`(필수 표기), `guidelineCdList`(타입 표기 — Array로 기재됐으나 실제는 문자열)에서 **3연속 불일치**. 프롬프트 작성 전 curl로 확인 |
| 실패가 오류로 오지 않는다 | `guidelineCdInfo`는 키 불일치 시 **빈 값을 200으로**(6.2절), `guidelineCdList`는 **HTML 페이지를**(4-3.2절) 반환. `msgCd: E`만 실패로 판정하면 놓친다 |
| 인증 헤더 4종 | 빈 값으로 정상 동작(2026-08-18). **키 자체는 삭제하지 말 것** |
| POST 계열 Read Timeout | 30000ms. 10000ms 이하에서 타임아웃 실패 |
| POST 계열 Query Params | 반드시 비워둘 것 |
| 출력 노드 앞 경유 | `AI_MESSAGE`를 만들 수 있는 노드는 **Language Model뿐**. Agent 직결 불가 |
| 개행 보존 | LM 경유 시 개행 소실(REQ-019). 승인과 개행 보존은 동시 획득 불가 |

---

## 9. 실행 계획

| 순서 | 작업 | 산출물 | 근거 절 | 상태 |
|---|---|---|---|---|
| 0 | **응답 volume 측정** | 규모·비용·필터 유효성 실측 | 4-1절 | ✅ **완료 (2026-08-20)** |
| 1 | ~~일반 모드 Body 확인~~ | 일반 모드 자체가 부재 | 8.1절 | ✅ **기각 확정** |
| 2 | **조직 규모 재측정** | `mgmtOrgId` 유효 확정, 실자산 112건 | 4-2절 | ✅ **완료 (2026-08-21)** |
| 3 | 🔴 **운영기 실규모 측정** | 조직 단위 워크플로우 성립 확정 | 7.2절 | ⏳ **다음** — 망 접근 필요 |
| 4 | **계층 0 분리** — W0-1 조직 조회 플로우 | 런타임 체인 5→4단계 축소 | 6.2절 | ✅ **운영기 실행 검증 완료 (2026-08-21)** |
| 4-b | ~~W0-2 가이드 사전 적재~~ | `aresultNo` 종속으로 불가 | 6.2절 | ❌ **기각** |
| 4-c | **W0-0 참조값 후보 조회** | `templateNo` 선택 근거 확보 | 4-3절 | ✅ **JSON 완료** — 캔버스 실행 남음 |
| 5 | **기능 시범** — `asstCode` 직접 지정, 수십 건 규모 | 파이프라인 정상 동작 증명 + **W2 배치 크기·조치가이드 방식 A/B 확정** | 4-2.6·4-3.3·6.4절 | ⏳ 개발기 가능 |
| 6 | **W1/W2 재조립** — 조직 단위 실행 구조 | 신규 워크플로우 JSON | 6.3·6.4절 | ⏳ 3·5번 이후 |
| 7 | **교정 측정** — 호출 1회 비용·증가 곡선 | 규칙 ①(제곱 누적) 확정 | 4절 | ⏳ 낮음 |
| 8 | **플랫폼 요구사항 제기** — REQ-004 상향 등 | 캔버스로 메울 수 없는 항목 이관 | 7.2·8.1절 | ⏳ |
| 9 | **운영기 담당자 데이터 확인** | 담당자 축 설계 가능 여부 | 4-1.4절 | ⏳ 3번과 동시 수행 |

### 착수 우선순위

**3번(운영기 실규모 측정)이 최우선이다.** 4-2절로 캔버스 단독 성립 가능성이 확인됐으나, 그 근거가 개발기 garbage 보정을 거친 **추정값**이다. 운영기 수치가 확정되어야 6번 재조립의 배치 크기를 정할 수 있다. 9번(담당자 데이터)도 같은 망 접근이 필요하므로 **한 번에 수행한다.**

| 결과 | 영향 |
|---|---|
| 조직당 **1,400건 이하** | 6절 구조가 조직 단위로 성립. 6번 재조립으로 직행 |
| 조직당 **수천 건** | `asstCode` 배치 축 병용 필요. 8번(플랫폼 요구사항)의 비중 상승 |

**4번(계층 0 분리)과 5번(기능 시범)은 3번과 무관하게 지금 착수 가능하다.**

- 4번은 4차 실패(#1A `orgList` 재귀가 1,017,179 토큰 소진)의 직접 원인 제거다
- 5번은 개발기 garbage를 우회하는 유일한 검증 경로다 — `mgmtOrgId` 대신 `asstCode`를 직접 지정하면 garbage가 딸려오지 않으므로 3~5단계 파이프라인을 개발기에서 완주 검증할 수 있다

**7번(교정 측정)의 우선순위는 낮다.** 4-1.5절에서 규칙 ①이 관측값과 정합함을 간접 확인했고(4회 제곱누적 1,210,850 ≈ 관측 1,324,532), 조직 규모가 1페이지 수준이면 어느 쪽이든 1회 실행으로 결론이 같다.

### 이 계획이 무효화되는 조건

| 조건 | 영향 |
|---|---|
| 4번 측정에서 증가가 **선형**으로 확인 | Agent당 호출 상한 3회 → 7회로 완화. 조직당 허용 규모 2배 이상 상향 |
| 운영기에서 `filter`가 **동작** | 미조치 35%만 반환 → 응답 원문 65% 감소 |
| REQ-004(일반 모드) **구현** | 계층 1을 LLM 없이 구성 가능. 단 Loop/Function 부재는 그대로 |
| ~~8.1 일반 모드 Body 지원~~ | ❌ **기각됨** (8.1절 — 모드 자체가 부재) |
| ~~8.2 방식 B 서버 필터 동작~~ | ❌ **기각됨** (4-1.1절) |

---

## 10. 문서 갱신 필요 항목

본 계획 확정에 따라 기존 문서에서 정정이 필요한 부분이다.

| 문서 | 항목 | 조치 |
|---|---|---|
| `stage5-test-guide.md` 6절 | "#1A 하드코딩 되돌리기" | **철회** — 별도 플로우 분리로 대체 (6.2절) |
| `11-flow-a-node-detail-config.md` | 예산 `100,000` 표기 | `945,000`으로 정정 (1.1절) |
| `11-flow-a-node-detail-config.md` | "방법 3 = 외부 배치 스크립트" | **삭제** — 캔버스 외부 수단은 범위 밖 (7.2절) |
| `11-flow-a-node-detail-config.md` | System Prompt의 `{값}` 표기 광범위 사용 | 중괄호 제거 (8.4절) |
| `04-ixi-enterprise-node-catalog.md` 420~450행 | API Request 일반 모드·`Data` 출력 포트를 "✅ 확인"으로 기재 | 🔴 **오류 정정** — 실재하지 않음. Tool 모드만 유효함을 명기 (8.1절) |
| `07-ixi-enterprise-requirements-spec.md` REQ-004 | 🟡 Medium | 🔴 **High로 상향** + 근거 3개 기재 (8.1절) |
| `08-ivms_openapi_spec.md` | `org_000991` 상위 조직 `org_000001` | 실측값 `org_002205`로 정정 |
| `08-ivms_openapi_spec.md` 2.2절 | `filter/xorStr` 스펙 서술 | **서버가 무시함**을 명기 (4-1.1절) |
| `11-flow-a-node-detail-config.md` 2-3절 | 방식 B "미검증" | **기각** 으로 확정 (4-1.1절) |
| `09-ivms-ixi-integration-requirements-spec.md` 5.2절 | "외부 발송 노드 부재" 전제 | Send Mail Output 실재 반영 |
| 신규 기록 필요 | 개발기 담당자 데이터 부재 | 환경 차이표에 추가 (4-1.4절) |
| `08-ivms_openapi_spec.md` 4.6절 | `resultId` 타입 `Array[string]` | 🔴 **오류 정정** — 실제는 **문자열**. 배열로 보내면 HTML 반환 (4-3.2절) |
| `08-ivms_openapi_spec.md` 4.5절 | `guidelineCdInfo` 파라미터 서술 | `aresultNo` 종속으로 **사전 적재 불가**를 명기. 키 불일치 시 빈 값 200 반환도 함께 (6.2절) |
| 신규 기록 필요 | `templateNo`별 기준항목 조회 경로 부재 | IVMS 운영팀 문의 항목으로 등록 (4-3.1절) |
| 신규 기록 필요 | 개발기 조직 미기입 자산 약 9,700건 | 환경 차이표에 추가 (4-2.2절) |

---

## 11. 측정 산출물

| 파일 | 내용 |
|---|---|
| `ixi-enterprise/w01-org-lookup.json` | **W0-1 계층 0 워크플로우** (6노드). ✅ 운영기 실행 검증 완료 |
| `ixi-enterprise/build-w01.py` | W0-1 생성기 (재현 가능). stage1 기반 + 구조 검증 7종 내장 |
| `ixi-enterprise/w00-reference-list.json` | **W0-0 참조값 후보 조회 워크플로우** (6노드) |
| `ixi-enterprise/build-w00.py` | W0-0 생성기 (재현 가능) |
| `ixi-enterprise/w00-usage-guide.md` | **W0-0 사용 설명서** — 질문 텍스트 5종·기대 출력·한계·판단 가이드 |
| `ixi-enterprise/orgscale-debug.sh` | **4-2절 측정 (재현 가능).** S1 조직 트리 / S2 필터 유효성(가짜 ID 대조) / S3 조직별 실자산 / S4 garbage 구간 경계 / S5 예산 환산·성립 판정 |
| `ixi-enterprise/scanresult-debug.sh` | 3단계 `msgCd:E` 원인 규명 |
| `ixi-enterprise/guideline-debug.sh` | 4단계 `guidelineCdInfo` 선검증 |

**운영기 측정 방법**: `orgscale-debug.sh`의 `BASE_URL`을 운영기 주소로 바꿔 재실행한다. S2의 가짜 ID 건수가 0에 가까우면 garbage가 없는 것이므로 S3의 "반환건수"가 곧 실자산이다.

> 4-1절 측정에 사용한 `volume-debug.py`는 삭제했다. 수치는 4-1절에 보존되어 있다.

### 11.1 측정 시 반드시 지킬 것 — 가짜 ID 대조

4-1.2절이 `mgmtOrgId`를 "유효"로 판정한 뒤 4-2.1절에서 뒤집힌 원인은 **대조군 부재** 하나다. 조직별 건수가 서로 다르다는 사실만으로 필터가 동작한다고 판단했고, 그 차이의 98.8%가 필터와 무관한 garbage였다.

> **필터 파라미터의 유효성을 재려면, 반드시 존재하지 않는 값으로도 호출해 그 결과를 기준선으로 삼을 것.**

`filter.xorStr` 기각(4-1.1절)은 MD5 대조로 확정됐고, `mgmtOrgId` 확정(4-2.1절)은 가짜 ID 대조로 이루어졌다. 대조군 없는 측정은 네 번의 감(勘) 추정과 같은 실패를 반복한다.
